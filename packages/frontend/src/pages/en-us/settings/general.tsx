import Layout from '../../../components/layout'
import GeneralSettings from '../../../components/settings/general-settings'

export default () => {
  return (
    <Layout meta={{ title: 'General Settings' }}>
      <GeneralSettings />
    </Layout>
  )
}
