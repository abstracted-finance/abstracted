import CompoundDashboard from '../../../components/dashboard/compound'

import Layout from '../../../components/layout'

export default () => {
  return (
    <Layout meta={{ title: 'Compound' }}>
      <CompoundDashboard />
    </Layout>
  )
}
