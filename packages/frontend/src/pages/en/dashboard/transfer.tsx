import Transfer from '../../../components/dashboard/transfer'

import Layout from '../../../components/layout'

export default () => {
  return (
    <Layout meta={{ title: 'Transfer' }}>
      <Transfer />
    </Layout>
  )
}
