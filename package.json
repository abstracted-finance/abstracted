{
  "name": "abstracted",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "build:frontend": "lerna bootstrap && cd packages/frontend && npm run build"
  },
  "workspaces": [
    "packages/*"
  ]
}
